version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -m streamlit run text_counter.py --server.port=8080 --server.address=0.0.0.0
  network:
    port: 8080
